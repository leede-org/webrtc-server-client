{
  "name": "@leede/web-hybrid-socket",
  "version": "1.0.0",
  "description": "",
  "main": "example/dist/server/demo_server.js",
  "files": [
    "client/dist",
    "server/dist",
    "example/dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "run-s install:*",
    "install:client": "npm --prefix ./client install ./client",
    "install:server": "npm --prefix ./server install ./server",
    "install:example": "npm --prefix ./example install ./example",
    "dev": "run-p dev:*",
    "dev:client": "npm --prefix ./client run dev",
    "dev:server": "npm --prefix ./server run dev",
    "dev:example": "npm --prefix ./example run dev",
    "build": "run-s build:client build:server build:example",
    "build:client": "npm --prefix ./client run build",
    "build:server": "npm --prefix ./server run build",
    "build:example": "npm --prefix ./example run build",
    "pack": "npm-pack-zip"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Leede-OU/web-hybrid-socket.git"
  },
  "author": "Leede",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Leede-OU/web-hybrid-socket/issues"
  },
  "homepage": "https://github.com/Leede-OU/web-hybrid-socket#readme",
  "devDependencies": {
    "npm-pack-zip": "^1.3.0",
    "npm-run-all": "^4.1.5"
  }
}
